# Database Scripts

Place owner-supplied and verified database test wrappers here only after the script path, arguments, timeout, exit-code semantics, and evidence contract are approved.

The current implementation includes a `DatabaseValidator` adapter and fixture tests. It does not invent or execute a production database script.
