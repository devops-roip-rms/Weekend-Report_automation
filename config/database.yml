{
  "collection_mode": "live",
  "adapter": "existing_powershell_script",
  "script": {
    # Existing approved database synchronization validation script.
    # The script is stored as part of the Weekend Report project.
    # The script owns:
    #   - database connections
    #   - database topology
    #   - test-table creation
    #   - synchronization verification
    #   - test-table deletion
    #   - cleanup
    # Weekend Report only executes the script and evaluates
    # the result returned by it.
    "path": "scripts/database/database_sync_check.ps1",
    # Maximum time Weekend Report allows the entire script
    # execution to run before treating it as an ERROR.
    "execution_timeout_seconds": 300
  },
  "algorithm": {
    "description": "Execute the approved existing database synchronization script exactly as provided.",
    "rewrite_algorithm": false
  }
}